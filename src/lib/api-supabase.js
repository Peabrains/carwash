import { supabase, supabaseConfigured, finishSupabaseRedirect, getSupabaseUser, signInStaffWithGoogle as supabaseGoogleSignIn, signOutSupabase, watchSupabaseUser, watchOperationalChanges as watchSupabaseOperationalChanges } from './supabase.js';

export const DEFAULT_PROVIDER_ID = 'washpoint';
export const DEFAULT_LOCATION_ID = 'washpoint-main';

const MOCK_SETTINGS = { min_lead_minutes: 60, max_advance_days: 14, buffer_minutes: 15, weekday_open: '08:00', weekday_close: '19:00', weekend_open: '08:00', weekend_close: '21:00' };
const TENANT_STORAGE_KEY = 'docket.activeTenant';
let activeTenant = loadTenant();

function loadTenant() { try { return JSON.parse(localStorage.getItem(TENANT_STORAGE_KEY)) || { providerId: DEFAULT_PROVIDER_ID, locationId: DEFAULT_LOCATION_ID }; } catch { return { providerId: DEFAULT_PROVIDER_ID, locationId: DEFAULT_LOCATION_ID }; } }
function ensureSupabase() { if (!supabaseConfigured || !supabase) throw new Error('Supabase is not configured.'); return supabase; }
function scope() { return { provider_id: activeTenant.providerId, location_id: activeTenant.locationId }; }
function errorOrThrow(error, operation) { if (error) throw new Error(`Supabase ${operation} failed: ${error.message || 'unknown error'}`); }
function asDate(value) { return value?.toDate ? value.toDate() : new Date(value); }
function overlaps(startA, endA, startB, endB) { return startA < endB && startB < endA; }
function minutes(value) { const [hours, mins] = String(value).slice(0, 5).split(':').map(Number); return hours * 60 + mins; }
function localDate() { return new Intl.DateTimeFormat('en-CA', { timeZone: 'Asia/Kuala_Lumpur', year: 'numeric', month: '2-digit', day: '2-digit' }).format(new Date()); }
function isBookable(dateISO, settings) { const today = localDate(); const start = new Date(`${today}T00:00:00+08:00`); const date = new Date(`${dateISO}T00:00:00+08:00`); const days = Math.round((date - start) / 86400000); return days >= 0 && days <= Number(settings.max_advance_days || 14); }
async function rows(table, columns = '*') { const db = ensureSupabase(); const { data, error } = await db.from(table).select(columns).eq('provider_id', activeTenant.providerId).eq('location_id', activeTenant.locationId); errorOrThrow(error, `loading ${table}`); return data || []; }

export const isSupabaseMode = true;
export function getActiveTenant() { return { ...activeTenant }; }
export function setActiveTenant(providerId, locationId) { activeTenant = { providerId, locationId }; localStorage.setItem(TENANT_STORAGE_KEY, JSON.stringify(activeTenant)); return getActiveTenant(); }
export function watchOperationalChanges(callback) { return watchSupabaseOperationalChanges(activeTenant.providerId, activeTenant.locationId, callback); }

export async function getAccessibleTenants() {
  const db = ensureSupabase();
  const [{ data: providers, error: providerError }, { data: locations, error: locationError }] = await Promise.all([
    db.from('providers').select('*').order('name'),
    db.from('locations').select('*').eq('is_active', true).order('name'),
  ]);
  errorOrThrow(providerError, 'loading providers'); errorOrThrow(locationError, 'loading locations');
  const valid = (locations || []).some(item => item.id === activeTenant.locationId && item.provider_id === activeTenant.providerId);
  if (!valid && locations?.[0]) setActiveTenant(locations[0].provider_id, locations[0].id);
  return { providers: providers || [], locations: locations || [] };
}

export async function getPlatformAdminData() {
  const db = ensureSupabase();
  const [{ data: providers, error: providerError }, { data: locations, error: locationError }, { data: staff, error: staffError }, { data: subscriptions, error: subscriptionError }, { data: onboarding, error: onboardingError }, { data: plans, error: planError }] = await Promise.all([
    db.from('providers').select('*').order('name'),
    db.from('locations').select('id,provider_id,name,is_active'),
    db.from('staff').select('id,provider_id,role,is_active'),
    db.from('provider_subscriptions').select('provider_id,plan_id,status,trial_ends_at,current_period_end'),
    db.from('provider_onboarding').select('*'),
    db.from('subscription_plans').select('*').order('monthly_price_myr'),
  ]);
  errorOrThrow(providerError, 'loading platform providers');
  errorOrThrow(locationError, 'loading platform locations');
  errorOrThrow(staffError, 'loading platform staff');
  errorOrThrow(subscriptionError, 'loading provider subscriptions');
  errorOrThrow(onboardingError, 'loading provider onboarding');
  errorOrThrow(planError, 'loading subscription plans');
  return { providers: providers || [], locations: locations || [], staff: staff || [], subscriptions: subscriptions || [], onboarding: onboarding || [], plans: plans || [] };
}

export async function saveSubscriptionPlan({ id, name, description = '', monthlyPriceMyr, maxLocations, maxStaff, maxMonthlyBookings, isActive = true }) {
  const db = ensureSupabase();
  const payload = { id: id?.trim() || name.toLowerCase().trim().replace(/[^a-z0-9]+/g, '-'), name: name.trim(), description: description.trim(), monthly_price_myr: Number(monthlyPriceMyr), max_locations: maxLocations ? Number(maxLocations) : null, max_staff: maxStaff ? Number(maxStaff) : null, max_monthly_bookings: maxMonthlyBookings ? Number(maxMonthlyBookings) : null, is_active: isActive, updated_at: new Date().toISOString() };
  const { data, error } = await db.from('subscription_plans').upsert(payload).select().single();
  errorOrThrow(error, 'saving subscription plan');
  return data;
}

export async function saveProviderSubscription({ providerId, planId, status = 'trialing' }) {
  const db = ensureSupabase();
  const now = new Date().toISOString();
  const payload = { provider_id: providerId, plan_id: planId || null, status, updated_at: now };
  const { data, error } = await db.from('provider_subscriptions').upsert(payload, { onConflict: 'provider_id' }).select().single();
  errorOrThrow(error, 'saving provider subscription');
  return data;
}

export async function createProvider({ name, description = '' }) { const db = ensureSupabase(); const id = name.toLowerCase().trim().replace(/[^a-z0-9]+/g, '-'); const { data, error } = await db.from('providers').insert({ id, name: name.trim(), description: description.trim(), status: 'active' }).select().single(); errorOrThrow(error, 'creating provider'); const locationId = `${id}-main`; const { data: location, error: locationError } = await db.from('locations').insert({ id: locationId, provider_id: id, name: 'Main outlet', timezone: 'Asia/Kuala_Lumpur', is_active: true }).select().single(); errorOrThrow(locationError, 'creating provider default location'); const { error: settingsError } = await db.from('booking_settings').upsert({ id: Date.now(), provider_id: id, location_id: locationId, ...MOCK_SETTINGS }, { onConflict: 'provider_id,location_id' }); errorOrThrow(settingsError, 'creating provider booking settings'); return { ...data, default_location: location }; }
export async function updateProvider(providerId, patch) { const db = ensureSupabase(); const { data, error } = await db.from('providers').update({ ...patch, updated_at: new Date().toISOString() }).eq('id', providerId).select().single(); errorOrThrow(error, 'updating provider'); return data; }
export async function createLocation({ providerId = activeTenant.providerId, name, address = '', timezone = 'Asia/Kuala_Lumpur' }) { const db = ensureSupabase(); const id = `${providerId}-${name.toLowerCase().trim().replace(/[^a-z0-9]+/g, '-')}`; const { data, error } = await db.from('locations').insert({ id, provider_id: providerId, name: name.trim(), address: address.trim(), timezone, is_active: true }).select().single(); errorOrThrow(error, 'creating location'); const { error: settingsError } = await db.from('booking_settings').upsert({ id: Date.now(), provider_id: providerId, location_id: id, ...MOCK_SETTINGS }, { onConflict: 'provider_id,location_id' }); errorOrThrow(settingsError, 'creating location booking settings'); return data; }
export async function updateLocation(locationId, patch) { const db = ensureSupabase(); const { data, error } = await db.from('locations').update({ ...patch, updated_at: new Date().toISOString() }).eq('id', locationId).select().single(); errorOrThrow(error, 'updating location'); return data; }
export async function listStaff() { const db = ensureSupabase(); const [{ data: staff, error: staffError }, { data: invitations, error: invitationError }] = await Promise.all([db.from('staff').select('*').eq('provider_id', activeTenant.providerId).or(`location_id.is.null,location_id.eq.${activeTenant.locationId}`).order('email'), db.from('staff_invitations').select('*').eq('provider_id', activeTenant.providerId).is('accepted_at', null).or(`location_id.is.null,location_id.eq.${activeTenant.locationId}`).order('email')]); errorOrThrow(staffError, 'loading staff'); errorOrThrow(invitationError, 'loading staff invitations'); return [...(staff || []), ...(invitations || []).map(item => ({ ...item, id: item.id, is_active: true, pending: true }))]; }
export async function saveStaff({ email, name = '', role = 'worker', isActive = true }) { const db = ensureSupabase(); const { data, error } = await db.rpc('invite_staff_member', { p_email: email.trim().toLowerCase(), p_name: name.trim(), p_role: role, p_provider_id: activeTenant.providerId, p_location_id: activeTenant.locationId, p_is_active: isActive }); errorOrThrow(error, 'saving staff invitation'); return data; }

export async function getServices({ includeInactive = false } = {}) { const db = ensureSupabase(); let request = db.from('services').select('*').eq('provider_id', activeTenant.providerId).eq('location_id', activeTenant.locationId).order('name'); if (!includeInactive) request = request.eq('is_active', true); const { data, error } = await request; errorOrThrow(error, 'loading services'); return data || []; }
export async function saveService({ id, name, durationMinutes, priceMyr, isActive = true }) { const db = ensureSupabase(); const payload = { ...scope(), name: name.trim(), duration_minutes: Number(durationMinutes), price_myr: Number(priceMyr), is_active: isActive, updated_at: new Date().toISOString() }; const { data, error } = id ? await db.from('services').update(payload).eq('id', id).select().single() : await db.from('services').insert(payload).select().single(); errorOrThrow(error, 'saving service'); return data; }
export async function getActiveBays({ includeInactive = false } = {}) { const db = ensureSupabase(); let request = db.from('bays').select('*').eq('provider_id', activeTenant.providerId).eq('location_id', activeTenant.locationId).order('name'); if (!includeInactive) request = request.eq('is_active', true); const { data, error } = await request; errorOrThrow(error, 'loading bays'); return data || []; }
export async function saveBay({ id, name, isActive = true, status = 'open' }) { const db = ensureSupabase(); const payload = { ...scope(), name: name.trim(), is_active: isActive, status, updated_at: new Date().toISOString() }; const { data, error } = id ? await db.from('bays').update(payload).eq('id', id).select().single() : await db.from('bays').insert(payload).select().single(); errorOrThrow(error, 'saving bay'); return data; }

export async function getAppointmentsForDate(dateISO) { const db = ensureSupabase(); const [{ data: appointments, error: appointmentError }, services, bays] = await Promise.all([db.from('appointments').select('*').eq('provider_id', activeTenant.providerId).eq('location_id', activeTenant.locationId).eq('scheduled_date', dateISO).is('archived_at', null).neq('status', 'cancelled').order('scheduled_at'), getServices({ includeInactive: true }), getActiveBays({ includeInactive: true })]); errorOrThrow(appointmentError, 'loading appointments'); const serviceMap = Object.fromEntries(services.map(item => [item.id, item])); const bayMap = Object.fromEntries(bays.map(item => [item.id, item])); return (appointments || []).map(item => ({ ...item, services: serviceMap[item.service_id], bays: bayMap[item.bay_id] })); }
async function recordBookingEvent(appointment, eventType, description, oldValue = null, newValue = null) { const db = ensureSupabase(); const user = await getSupabaseUser(); const { error } = await db.from('booking_events').insert({ ...scope(), appointment_id: appointment.id, reference: appointment.reference, event_type: eventType, description: description || '', old_value: oldValue, new_value: newValue, actor_id: user?.id || null }); errorOrThrow(error, 'recording booking history'); }
export async function getBookingHistory() { const db = ensureSupabase(); const [{ data: appointments, error: appointmentError }, services, bays] = await Promise.all([db.from('appointments').select('*').eq('provider_id', activeTenant.providerId).eq('location_id', activeTenant.locationId).order('scheduled_at', { ascending: false }).limit(300), getServices({ includeInactive: true }), getActiveBays({ includeInactive: true })]); errorOrThrow(appointmentError, 'loading booking history'); const serviceMap = Object.fromEntries(services.map(item => [item.id, item])); const bayMap = Object.fromEntries(bays.map(item => [item.id, item])); const values = (appointments || []).map(item => ({ ...item, services: serviceMap[item.service_id], bays: bayMap[item.bay_id] })); const ids = values.map(item => item.id); if (!ids.length) return values.map(item => ({ ...item, events: [] })); const { data: events, error: eventError } = await db.from('booking_events').select('*').in('appointment_id', ids).order('created_at', { ascending: false }); errorOrThrow(eventError, 'loading booking history events'); const eventMap = {}; for (const event of events || []) (eventMap[event.appointment_id] ??= []).push(event); return values.map(item => ({ ...item, events: eventMap[item.id] || [] })); }
export async function getBayClosuresForDate(dateISO) { const db = ensureSupabase(); const { data, error } = await db.from('bay_closures').select('*').eq('provider_id', activeTenant.providerId).eq('location_id', activeTenant.locationId).lt('starts_at', `${dateISO}T23:59:59+08:00`).gt('ends_at', `${dateISO}T00:00:00+08:00`).order('starts_at'); errorOrThrow(error, 'loading bay closures'); const bays = await getActiveBays({ includeInactive: true }); const bayMap = Object.fromEntries(bays.map(item => [item.id, item])); return (data || []).map(item => ({ ...item, bays: bayMap[item.bay_id] })); }
export async function getBookingSettings() { const db = ensureSupabase(); const { data, error } = await db.from('booking_settings').select('*').eq('provider_id', activeTenant.providerId).eq('location_id', activeTenant.locationId).maybeSingle(); errorOrThrow(error, 'loading booking settings'); return { ...MOCK_SETTINGS, ...scope(), ...(data || {}) }; }
export async function updateBookingSettings(patch) { const db = ensureSupabase(); const { data, error } = await db.from('booking_settings').update({ ...patch, ...scope(), updated_at: new Date().toISOString() }).eq('provider_id', activeTenant.providerId).eq('location_id', activeTenant.locationId).select().single(); errorOrThrow(error, 'updating booking settings'); return data; }
export async function getCrewBreaks() { const bays = await getActiveBays({ includeInactive: true }); const bayMap = Object.fromEntries(bays.map(item => [item.id, item])); return (await rows('crew_break_schedule')).map(item => ({ ...item, bays: bayMap[item.bay_id] })); }
export async function setCrewBreak({ bayId, startTime, durationMinutes }) { const db = ensureSupabase(); const { data, error } = await db.from('crew_break_schedule').insert({ ...scope(), bay_id: bayId, start_time: startTime, duration_minutes: Number(durationMinutes) }).select().single(); errorOrThrow(error, 'creating crew break'); return data; }
export async function removeCrewBreak(id) { const db = ensureSupabase(); const { error } = await db.from('crew_break_schedule').delete().eq('id', id); errorOrThrow(error, 'deleting crew break'); }

export async function getAvailableSlots(dateISO, serviceId, excludeAppointmentId = null) {
  const [bays, services, settings, loadedAppointments, closures, breaks] = await Promise.all([getActiveBays(), getServices(), getBookingSettings(), getAppointmentsForDate(dateISO), getBayClosuresForDate(dateISO), getCrewBreaks()]);
  const appointments = loadedAppointments.filter(item => item.id !== excludeAppointmentId);
  const service = services.find(item => item.id === serviceId); if (!service || !isBookable(dateISO, settings)) return [];
  const date = new Date(`${dateISO}T12:00:00+08:00`); const weekend = [0, 6].includes(date.getUTCDay()); const opening = weekend ? settings.weekend_open : settings.weekday_open; const closing = weekend ? settings.weekend_close : settings.weekday_close; const slots = [];
  for (let minute = minutes(opening); minute + Number(service.duration_minutes) <= minutes(closing); minute += 30) {
    const time = `${String(Math.floor(minute / 60)).padStart(2, '0')}:${String(minute % 60).padStart(2, '0')}`; const start = new Date(`${dateISO}T${time}:00+08:00`); const end = new Date(start.getTime() + Number(service.duration_minutes) * 60000); if (start.getTime() < Date.now() + Number(settings.min_lead_minutes) * 60000) continue;
    const freeBays = bays.filter(bay => (!bay.status || bay.status === 'open') && !appointments.some(item => item.bay_id === bay.id && overlaps(start, new Date(end.getTime() + Number(settings.buffer_minutes || 0) * 60000), asDate(item.scheduled_at), new Date(asDate(item.scheduled_at).getTime() + Number(item.duration_minutes) * 60000 + Number(settings.buffer_minutes || 0) * 60000))) && !breaks.some(item => item.bay_id === bay.id && overlaps(start, end, new Date(`${dateISO}T${String(item.start_time).slice(0, 5)}:00+08:00`), new Date(new Date(`${dateISO}T${String(item.start_time).slice(0, 5)}:00+08:00`).getTime() + Number(item.duration_minutes) * 60000))) && !closures.some(item => item.bay_id === bay.id && overlaps(start, end, asDate(item.starts_at), asDate(item.ends_at))));
    slots.push({ time, available: Boolean(freeBays.length), bayId: freeBays[0]?.id, bayIds: freeBays.map(item => item.id) });
  }
  return slots;
}

export async function resolveAppointmentAttention(id, { description = '', archive = true } = {}) { const db = ensureSupabase(); const { data: current, error: currentError } = await db.from('appointments').select('*').eq('id', id).single(); errorOrThrow(currentError, 'loading appointment for resolution'); const patch = { needs_attention: false, updated_at: new Date().toISOString(), ...(archive ? { archived_at: new Date().toISOString() } : {}) }; const { data, error } = await db.from('appointments').update(patch).eq('id', id).select().single(); errorOrThrow(error, 'resolving appointment attention'); await recordBookingEvent(current, 'resolved', description || 'Attention item resolved by staff', { needs_attention: true }, { needs_attention: false }); if (archive) await recordBookingEvent(data, 'archived', 'Resolved booking removed from the active calendar', { archived_at: null }, { archived_at: data.archived_at }); return data; }
export async function rescheduleAppointment(id, { dateISO, time, bayId } = {}) { const db = ensureSupabase(); const { data: current, error: currentError } = await db.from('appointments').select('*').eq('id', id).single(); errorOrThrow(currentError, 'loading appointment for rescheduling'); const { data: moved, error } = await db.rpc('reschedule_appointment_atomic', { p_appointment_id: id, p_scheduled_date: dateISO, p_time: time, p_bay_id: bayId || null }); errorOrThrow(error, 'rescheduling appointment'); const result = Array.isArray(moved) ? moved[0] : moved; if (!result) throw new Error('The booking could not be moved.'); const scheduledAt = result.result_scheduled_at; const { data, error: reloadError } = await db.from('appointments').select('*').eq('id', id).single(); errorOrThrow(reloadError, 'loading moved appointment'); await recordBookingEvent(data, 'rescheduled', `Booking moved to ${dateISO} at ${time}`, { scheduled_date: current.scheduled_date, scheduled_at: current.scheduled_at, bay_id: current.bay_id }, { scheduled_date: dateISO, scheduled_at: scheduledAt, bay_id: result.result_bay_id }); return data; }
export async function updateAppointmentStatus(id, status, description = '') { const db = ensureSupabase(); const { data: current, error: currentError } = await db.from('appointments').select('*').eq('id', id).single(); errorOrThrow(currentError, 'loading appointment for status update'); const { data, error } = await db.from('appointments').update({ status, updated_at: new Date().toISOString() }).eq('id', id).select().single(); errorOrThrow(error, 'updating appointment status'); await recordBookingEvent(data, 'status_changed', description || `Status changed to ${status}`, { status: current.status }, { status }); return data; }
export async function archiveAppointment(id, description = 'Booking archived from the active calendar') { const db = ensureSupabase(); const { data: current, error: currentError } = await db.from('appointments').select('*').eq('id', id).single(); errorOrThrow(currentError, 'loading appointment for archival'); const archivedAt = new Date().toISOString(); const { data, error } = await db.from('appointments').update({ archived_at: archivedAt, updated_at: archivedAt }).eq('id', id).select().single(); errorOrThrow(error, 'archiving appointment'); await recordBookingEvent(data, 'archived', description, { archived_at: current.archived_at }, { archived_at: archivedAt }); return data; }

async function flagAppointmentsForOutage(closure) { const db = ensureSupabase(); const start = new Date(closure.starts_at); const end = new Date(closure.ends_at); const dateISO = new Intl.DateTimeFormat('en-CA', { timeZone: 'Asia/Kuala_Lumpur' }).format(start); const { data: appointments, error: appointmentError } = await db.from('appointments').select('*').eq('provider_id', activeTenant.providerId).eq('location_id', activeTenant.locationId).eq('scheduled_date', dateISO).neq('status', 'cancelled'); errorOrThrow(appointmentError, 'checking outage appointments'); const atRisk = (appointments || []).filter(item => item.bay_id === closure.bay_id && overlaps(start, end, asDate(item.scheduled_at), new Date(asDate(item.scheduled_at).getTime() + Number(item.duration_minutes) * 60000))); await Promise.all(atRisk.map(async item => { const needsNewAttention = !item.needs_attention || item.archived_at; const patch = { needs_attention: true, archived_at: null, updated_at: new Date().toISOString() }; const { data: updated, error: updateError } = await db.from('appointments').update(patch).eq('id', item.id).select().single(); errorOrThrow(updateError, 'flagging outage appointment'); if (needsNewAttention) await recordBookingEvent(updated, 'outage_flagged', `Booking affected by bay outage${closure.reason ? `: ${closure.reason}` : ''}`, { needs_attention: Boolean(item.needs_attention), archived_at: item.archived_at || null }, { needs_attention: true, archived_at: null }); })); return atRisk.length; }
export async function reportBayDown(bayId, { startsAt, endsAt, reason } = {}) { const db = ensureSupabase(); const { data, error } = await db.from('bay_closures').insert({ ...scope(), bay_id: bayId, starts_at: startsAt, ends_at: endsAt, reason: reason || null }).select().single(); errorOrThrow(error, 'reporting bay outage'); const flagged = await flagAppointmentsForOutage(data); return { flagged, id: data.id }; }
export async function updateBayClosure(closureId, { startsAt, endsAt, reason } = {}) { const db = ensureSupabase(); const { data, error } = await db.from('bay_closures').update({ starts_at: startsAt, ends_at: endsAt, reason: reason || null }).eq('id', closureId).select().single(); errorOrThrow(error, 'updating bay outage'); const flagged = await flagAppointmentsForOutage(data); return { ...data, flagged }; }
export async function clearBayClosure(closureId) { const db = ensureSupabase(); const { data, error } = await db.from('bay_closures').delete().eq('id', closureId).select('id'); errorOrThrow(error, 'clearing bay outage'); if (!data?.length) throw new Error('The outage was not deleted. It may already be gone or you may not have permission.'); }
export async function bringBayUp(bayId) { const db = ensureSupabase(); const { data, error } = await db.from('bays').update({ status: 'open', updated_at: new Date().toISOString() }).eq('id', bayId).select().single(); errorOrThrow(error, 'bringing bay back up'); return data; }

export async function signInStaff() { return supabaseGoogleSignIn(); }
export async function getAuthUser() { return getSupabaseUser(); }
export async function getCurrentStaff() { const user = await getSupabaseUser(); if (!user) return null; const db = ensureSupabase(); const { data, error } = await db.rpc('accept_staff_invitation'); errorOrThrow(error, 'loading current staff'); return data || null; }
export async function signOutStaff() { return signOutSupabase(); }
export function watchStaffAuth(callback) { return watchSupabaseUser(callback); }
export async function finishStaffRedirect() { return finishSupabaseRedirect(); }
