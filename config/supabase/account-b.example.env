# Supabase profile B — fill locally, never commit the matching .env file.
SUPABASE_ACCOUNT_LABEL=account-b
SUPABASE_PROJECT_REF=
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=

# Optional: only for local CLI/automation. Never use a service_role key here.
SUPABASE_ACCESS_TOKEN=
